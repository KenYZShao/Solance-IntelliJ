import com.solance.domain.Transaction;
import com.solance.dto.PaymentRequest;
import org.junit.jupiter.api.Test;
import org.springframework.transaction.annotation.Transactional;
/*
@Test
@Transactional
void testProcessPayment() {
    PaymentRequest request = new PaymentRequest();
    // ... 设置请求参数 ...

    Transaction txn = workflowService.processPayment(request);

    assertEquals(TransactionStatus.PROCESSED, txn.getStatus());
    assertNotNull(txn.getId());
}*/